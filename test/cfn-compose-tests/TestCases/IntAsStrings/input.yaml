responses:
  200:
    description: A normal output name / old
    schema:
      properties:
        Name:
          type: string
        Old:
          type: boolean
  "404":
    description: not found
    schema:
      properties:
        Name:
          type: string